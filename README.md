# Compilation

To compile this manuscript, please use the following command in your favorite terminal:

```bash
sh compile.sh
```

# Requirements
You may need the complete latex compilation tool chain to compile this manuscript. In a modern `Ubuntu` operating system or any other Linux operating system supporting `apt-get`, you can install the required tools by using the following command in your favorite terminal program:

```bash
sudo apt install texlive-full
```